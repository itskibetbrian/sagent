<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="Theme.App.SplashScreen" parent="AppTheme" tools:override="true">
    <!-- Intentionally empty for API-specific attributes to avoid linking errors.
         The app theme is applied in MainActivity at runtime. -->
  </style>
</resources>
